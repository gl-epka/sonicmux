#![doc = "Development task runner for the SonicMux workspace."]
#![forbid(unsafe_code)]

use anyhow::Result;

fn main() -> Result<()> {
    println!("SonicMux development tasks arrive with their owning milestones.");
    Ok(())
}
